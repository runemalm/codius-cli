public {{ method.returns }} {{ method.name }}({% for p in method.parameters %}{{ p.type }} {{ p.name }}{% if not loop.last %}, {% endif %}{% endfor %})
{
    {{ method.body | default("// TODO: implement") }}
}